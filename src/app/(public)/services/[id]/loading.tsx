export default function Loading() {
  // You can add any UI inside Loading, including a Skeleton.
  return (
    <div className="container flex h-full w-full items-center justify-center bg-yellow-500 bg-opacity-50 p-4 text-foreground">
      ...loading
    </div>
  );
}
