export default function Loading() {
  // You can add any UI inside Loading, including a Skeleton.
  return (
    <div className="w-full p-4 h-full flex justify-center items-center bg-yellow-500 bg-opacity-50 text-foreground">
      ...loading
    </div>
  );
}
